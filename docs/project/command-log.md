# Project command log

This log records meaningful setup and baseline commands. Paths and examples use
synthetic data and contain no credentials.

## Repository and machine setup

| Command or operation                                                                                                                        | Purpose                                                                  | Modification and reversal                                                                                                               |
| ------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------- |
| `git --version`, `gh --version`, `node --version`, `npm --version`, `yarn --version`, `docker --version`, `wsl --status`, and `nvm version` | Inventory the existing toolchain before installing anything              | Read-only                                                                                                                               |
| GitHub app account lookup and fork lookup                                                                                                   | Confirm the authenticated account and avoid creating a duplicate fork    | Read-only until fork creation                                                                                                           |
| Create `FPynk/actual` from `actualbudget/actual`                                                                                            | Establish the requested fork                                             | Creates the GitHub fork; it can be archived or deleted later in GitHub settings                                                         |
| `git clone https://github.com/FPynk/actual.git actual`                                                                                      | Create the project checkout in the workspace                             | Creates only `C:\Users\Andrew\Desktop\Side Projects\actual`; remove that verified directory to undo                                     |
| `git config --global --add safe.directory "C:/Users/Andrew/Desktop/Side Projects/actual"`                                                   | Allow Git to use the checkout after Windows ownership checks rejected it | Changes one global Git entry; undo with `git config --global --unset-all safe.directory "C:/Users/Andrew/Desktop/Side Projects/actual"` |
| `git remote add upstream https://github.com/actualbudget/actual.git`                                                                        | Preserve the official upstream relationship                              | Changes local `.git/config`; undo with `git remote remove upstream`                                                                     |
| `git fetch --all --prune`                                                                                                                   | Verify remote branches and the current upstream default                  | Updates only local remote-tracking references                                                                                           |
| `git switch -c integration/finance-app upstream/master`                                                                                     | Create the project integration branch without changing `master`          | Creates a local branch; delete it only after moving to another branch with `git branch -d integration/finance-app`                      |
| `git push --set-upstream origin integration/finance-app`                                                                                    | Publish the integration target for ticketed pull requests                | Creates the remote branch; it can be deleted after all dependent pull requests are closed                                               |
| `git config core.autocrlf false` and `git config core.eol lf`                                                                               | Preserve the repository's LF files during Windows validation             | Changes checkout-local Git configuration; undo with `git config --unset core.autocrlf` and `git config --unset core.eol`                |

## Runtime and dependencies

| Command or operation       | Purpose                                                                | Modification and reversal                                                                                                                |
| -------------------------- | ---------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| `nvm install 22.18.0`      | Install a repository-supported Node 22 runtime after detecting Node 25 | Adds one NVM-managed runtime; switch away and run `nvm uninstall 22.18.0` to remove it                                                   |
| `nvm use 22.18.0`          | Select Node 22 for this shell and the NVM symlink                      | Changes the active NVM runtime; reverse with `nvm use <previous-version>`                                                                |
| `yarn --version`           | Confirm `.yarn/releases/yarn-4.17.1.cjs` is active                     | Read-only                                                                                                                                |
| `yarn install --immutable` | Install exactly the dependencies declared by the upstream lockfile     | Populates repository cache/install state; no lockfile change. Yarn's project-local cache can be regenerated by running the command again |

## Baseline checks

| Command or operation                                                                                                                  | Purpose                                                                         | Modification and reversal                                                                           |
| ------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| `yarn oxfmt --check .`                                                                                                                | Verify formatting without rewriting files                                       | Read-only                                                                                           |
| `yarn oxfmt --check . && yarn oxlint --type-aware --quiet` through `yarn lint`                                                        | Run the upstream formatting and type-aware lint configuration                   | Read-only; exposed two unchanged Windows path findings                                              |
| `yarn typecheck`                                                                                                                      | Check TypeScript across all workspaces                                          | Produces only ignored tool caches; rerunning regenerates them                                       |
| `yarn test`                                                                                                                           | Run the upstream workspace tests                                                | Creates ignored test caches and a synthetic `test-budget` directory when the Windows teardown fails |
| `yarn workspace @actual-app/core vitest --run src/server/main.test.ts`                                                                | Reproduce the SQLite teardown failure in isolation                              | Recreates only the same synthetic test fixture                                                      |
| Resolve the absolute `packages\loot-core\src\mocks\files\budgets\test-budget` path, verify it is beneath the checkout, then remove it | Clean only test-generated state after the failed run                            | Deletes synthetic files created by the baseline test; the next test run recreates them              |
| `git diff --exit-code` and `git add --update` after confirming an empty diff                                                          | Prove snapshot contents were unchanged and refresh stale Windows index metadata | Updates only Git index stat information; no content was staged                                      |

## Build and UI verification

| Command or operation                                                                                               | Purpose                                                                                           | Modification and reversal                                                                                                                                             |
| ------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Run `yarn build:browser` from PowerShell                                                                           | Test the documented root build entry point                                                        | Failed before producing a usable build because Bash misread the Windows path                                                                                          |
| Invoke `bin/package-browser` with `C:\Program Files\Git\bin\bash.exe` and repository `node_modules\.bin` on `PATH` | Execute the Bash-based browser build with the minimum already-installed compatibility layer       | Produces ignored build output; a later build safely replaces it                                                                                                       |
| Allow `bin/package-browser` to clone or update `actualbudget/translations` at `packages/desktop-client/locale`     | Supply current localization assets required by the standard browser build                         | Creates or updates only the ignored nested translation checkout; if it was created by this run, verify its absolute path is beneath the repository before removing it |
| `node packages/desktop-client/bin/serve-build.mjs`                                                                 | Serve the built browser client on local port 3001                                                 | Starts a project-local process only; stop with `Ctrl+C` or the verified process ID                                                                                    |
| Browser automation against `http://127.0.0.1:3001`                                                                 | Verify the synthetic demo, reports, schedules, accounts, transactions, import dialog, and console | Read-only except for temporary browser profile and screenshot output                                                                                                  |
| Inspect Node process command lines, stop only the process running `packages/desktop-client/bin/serve-build.mjs`    | Ensure the local baseline server is stopped without touching unrelated Node tools                 | Stops only the verified project server; rerun the serve command to restore it                                                                                         |

## Safety notes

- No `.env` file, token, provider credential, statement, or personal financial
  data was created.
- No Docker installation or daemon change was made.
- No unrelated project was modified.
- Cleanup commands were limited to an absolute, verified synthetic fixture path.
- The two Git remotes remain `origin` for the fork and `upstream` for the
  official project.
